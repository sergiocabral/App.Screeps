import { ScheduledMessage } from '../../Schedule/Message/ScheduledMessage';

/**
 * Exibe as mensagens recebidas para o console como informações de debug.
 */
export class ShowDebugToConsole extends ScheduledMessage {}
